interface ChartData {
	data: { id: string; value: number }[]
	colorsArray?: string[]
	colorsSchema?: string
	margin?: {
		top?: number
		left?: number
		bottom?: number
		right?: number
	}
}

export const RaceChart = (_props: ChartData) => {
	return null
}