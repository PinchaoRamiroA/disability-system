import { Typography } from '@mui/material'
import { styled } from '@mui/system'

export const Title = styled(Typography)`
  font-weight: 700;
  align-self: center;
`

// export const Container = styled('li')<{ theme?: Theme }>(
//   ({ theme }) => `
//   display: flex;
//   gap: ${theme.spacing(1)};
// `
// )
